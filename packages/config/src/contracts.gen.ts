// Generated by @jarenjs/emit from packages/config/schemas.
// Do not edit: regenerate instead.

/**
 * Schema constraints this type cannot express: pattern="^[0-9a-f]{64}$"
 */
export type Sha256 = string;

/**
 * Lower-case hyphenated identifier — capability tags, slot names, ids.
 * Schema constraints this type cannot express: pattern="^[a-z0-9]+(-[a-z0-9]+)*$"
 */
export type Name = string;

export type SlotName = Name;

/**
 * Schema constraints this type cannot express: pattern="^[a-zA-Z0-9_-]{1,64}$"
 */
export type ToolName = string;

/**
 * An http(s) base URL with no userinfo, query or fragment — a URL that could smuggle a credential cannot validate.
 * Schema constraints this type cannot express: pattern="^https?://[^@?#\\s]+$"
 */
export type SafeBaseUrl = string;

/**
 * Sourced cost metadata. Absence on a candidate means cost UNKNOWN, never free.
 */
export interface RateCard {
  /**
   * Schema constraints this type cannot express: pattern="^[A-Z]{3}$"
   */
  currency: string;
  /**
   * Schema constraints this type cannot express: minimum=0
   */
  inputPerMillionTokens: number | null;
  /**
   * Schema constraints this type cannot express: minimum=0
   */
  outputPerMillionTokens: number | null;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  source: string;
  /**
   * Schema constraints this type cannot express: pattern="^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
   */
  asOf: string;
}


/**
 * Schema constraints this type cannot express: if={"properties":{"kind":{"const":"embedding"}}}, then={"required":["dims"]}
 */
export type CandidatePart1 = unknown;

/**
 * Schema constraints this type cannot express: if={"properties":{"kind":{"const":"chat"}}}, then={"not":{"required":["dims"]}}
 */
export type CandidatePart2 = unknown;

/**
 * Schema constraints this type cannot express: if={"properties":{"provider":{"const":"builtin"}}}, then={"properties":{"kind":{"const":"embedding"},"baseUrl":{"type":"null"},"credentialSlot":{"type":"null"}}}
 */
export type CandidatePart3 = unknown;

/**
 * Schema constraints this type cannot express: if={"properties":{"provider":{"const":"custom"}}}, then={"properties":{"baseUrl":{"$ref":"#/$defs/safeBaseUrl"}}}
 */
export type CandidatePart4 = unknown;

/**
 * A credential-free model candidate. `credentialSlot` names a slot; the value never appears. `builtin` is only an embedding provider (the offline reference embedder), and a `custom` provider must declare its base because no default exists for it.
 */
export type Candidate = CandidatePart1 & CandidatePart2 & CandidatePart3 & CandidatePart4 & { id: Name; kind: "chat" | "embedding"; provider: "openrouter" | "ollama" | "lmstudio" | "custom" | "builtin"; model: string; baseUrl: SafeBaseUrl | null; credentialSlot: SlotName | null; features: Array<Name>; dims?: number; rateCard: RateCard | null; limitations?: string; };

export interface CapabilityCandidatesItem {
  candidate: Name;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  priority: number;
}


export interface Capability {
  tag: Name;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  intent: string;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  candidates: Array<CapabilityCandidatesItem>;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  limitations: string;
}


/**
 * A versioned content reference — a prompt pack/template or a structured-output response schema. The revision is the canonical SHA-256 of the referenced content, computed by its owner.
 */
export interface ContentRef {
  id: Name;
  revision: Sha256;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  description: string;
}


/**
 * A public reference to a policy or ranker component owned by its domain package. The registry holds only the reference and revision; the values and the decision that selected them stay with the owner.
 */
export interface ComponentRef {
  id: Name;
  kind: "policy" | "ranker";
  /**
   * Schema constraints this type cannot express: pattern="^@(tangleai|jarenjs)/[a-z0-9-]+$"
   */
  package: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  export: string;
  revision: Sha256;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  description: string;
}


export interface InferencePresetRetryOneOf2 {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  attempts: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  baseMs: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  maxMs: number;
}


export interface InferencePresetReasoningOneOf2 {
  effort?: "none" | "minimal" | "low" | "medium" | "high";
  enabled?: boolean;
  exclude?: boolean;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxTokens?: number;
}


export interface InferencePreset {
  id: Name;
  /**
   * Schema constraints this type cannot express: minimum=0, maximum=2
   */
  temperature: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxTokens: number | null;
  maxTokensField?: "max_tokens" | "max_completion_tokens";
  retry: null | InferencePresetRetryOneOf2;
  reasoning: null | InferencePresetReasoningOneOf2;
}


/**
 * Requested ceilings. A profile may lower a host ceiling and can never raise one; the host's hard ceilings always win.
 */
export interface BudgetPreset {
  id: Name;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxCalls: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxTokens: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxMs: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxConcurrency: number | null;
}


/**
 * One chat role of a profile. Exactly one of `capability` (resolve by intent) or `candidate` (pin one) is set. Tools are requested names — the effective manifest is the intersection with the host allowlist; a request is never permission.
 */
export interface RoleSpec {
  capability: Name | null;
  candidate: Name | null;
  prompt: Name | null;
  responseSchema: Name | null;
  tools: Array<ToolName>;
  toolsRequired: boolean;
  inference: Name | null;
  ranker: Name | null;
}


export interface RootProfile {
  id: Name;
  kind: "root";
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  description: string;
  /**
   * Schema constraints this type cannot express: minProperties=1, propertyNames={"pattern":"^[a-z0-9]+(-[a-z0-9]+)*$"}
   */
  roles: { [key: string]: RoleSpec; };
  embedding: Name;
  policyComponent: Name | null;
  budget: Name | null;
}


/**
 * Single-parent inheritance: the RFC 7396 patch is applied to the RESOLVED parent by `@jarenjs/json.applyMergePatch`, and the merged document must validate as a root profile again. The patch is the one open object in this schema — its members are judged after the merge, so an undeclared member still cannot survive.
 */
export interface ChildProfile {
  id: Name;
  kind: "child";
  extends: Name;
  patch: { [key: string]: unknown; };
}


/**
 * The registry names credential-free candidates, extensible capability intents, prompt/response-schema/component revisions, inference and budget presets, and the profiles that select among them. A capability tag is a declared intent with ordered candidates and stated limitations, never an adjective proven by its spelling. Every object closes its properties so a member nobody declared — an API key, a token — cannot be carried at all; credential access is only ever a slot NAME. Cross-references are asserted here through `$query` at validation time, so a registry that names a candidate, prompt, component, preset or parent that does not exist cannot validate at all. Child profiles carry exactly one `extends` parent and an RFC 7396 merge patch; the merged result is validated against this same document again, which is why the patch member is the one deliberately open object in the file.
 * Schema constraints this type cannot express: $query={"$and":[{"$eq":[{"$count":"$.credentialSlots[*]"},{"$count":{"$distinct":"$.credentialSlots[*]"}}]},{"$eq":[{"$count":"$.candidates[*].id"},{"$count":{"$distinct":"$.candidates[*].id"}}]},{"$eq":[{"$count":"$.capabilities[*].tag"},{"$count":{"$distinct":"$.capabilities[*].tag"}}]},{"$eq":[{"$count":"$.prompts[*].id"},{"$count":{"$distinct":"$.prompts[*].id"}}]},{"$eq":[{"$count":"$.responseSchemas[*].id"},{"$count":{"$distinct":"$.responseSchemas[*].id"}}]},{"$eq":[{"$count":"$.components[*].id"},{"$count":{"$distinct":"$.components[*].id"}}]},{"$eq":[{"$count":"$.inference[*].id"},{"$count":{"$distinct":"$.inference[*].id"}}]},{"$eq":[{"$count":"$.budgets[*].id"},{"$count":{"$distinct":"$.budgets[*].id"}}]},{"$eq":[{"$count":"$.profiles[*].id"},{"$count":{"$distinct":"$.profiles[*].id"}}]},{"$every":{"c":"$.capabilities[*].candidates[*]"},"$satisfies":{"$exists":{"$index-of":["$.candidates[*].id","$c.candidate"]}}},{"$every":{"s":"$.candidates[*].credentialSlot"},"$satisfies":{"$or":[{"$eq":["$s",null]},{"$exists":{"$index-of":["$.credentialSlots[*]","$s"]}}]}},{"$every":{"p":"$.profiles[*]"},"$satisfies":{"$or":[{"$eq":["$p.kind","child"]},{"$exists":{"$index-of":["$.candidates[?(@.kind=='embedding')].id","$p.embedding"]}}]}},{"$every":{"r":"$.profiles[*].roles[*]"},"$satisfies":{"$or":[{"$eq":["$r.candidate",null]},{"$exists":{"$index-of":["$.candidates[?(@.kind=='chat')].id","$r.candidate"]}}]}},{"$every":{"r":"$.profiles[*].roles[*]"},"$satisfies":{"$or":[{"$eq":["$r.capability",null]},{"$exists":{"$index-of":["$.capabilities[*].tag","$r.capability"]}}]}},{"$every":{"r":"$.profiles[*].roles[*]"},"$satisfies":{"$not":{"$eq":[{"$eq":["$r.capability",null]},{"$eq":["$r.candidate",null]}]}}},{"$every":{"r":"$.profiles[*].roles[*]"},"$satisfies":{"$or":[{"$eq":["$r.prompt",null]},{"$exists":{"$index-of":["$.prompts[*].id","$r.prompt"]}}]}},{"$every":{"r":"$.profiles[*].roles[*]"},"$satisfies":{"$or":[{"$eq":["$r.responseSchema",null]},{"$exists":{"$index-of":["$.responseSchemas[*].id","$r.responseSchema"]}}]}},{"$every":{"r":"$.profiles[*].roles[*]"},"$satisfies":{"$or":[{"$eq":["$r.inference",null]},{"$exists":{"$index-of":["$.inference[*].id","$r.inference"]}}]}},{"$every":{"r":"$.profiles[*].roles[*]"},"$satisfies":{"$or":[{"$eq":["$r.ranker",null]},{"$exists":{"$index-of":["$.components[?(@.kind=='ranker')].id","$r.ranker"]}}]}},{"$every":{"p":"$.profiles[?(@.kind=='root')]"},"$satisfies":{"$or":[{"$eq":["$p.policyComponent",null]},{"$exists":{"$index-of":["$.components[?(@.kind=='policy')].id","$p.policyComponent"]}}]}},{"$every":{"p":"$.profiles[?(@.kind=='root')]"},"$satisfies":{"$or":[{"$eq":["$p.budget",null]},{"$exists":{"$index-of":["$.budgets[*].id","$p.budget"]}}]}},{"$every":{"p":"$.profiles[?(@.kind=='child')]"},"$satisfies":{"$exists":{"$index-of":["$.profiles[*].id","$p.extends"]}}}]}
 */
export interface ProfileRegistry {
  /**
   * Registry document format version.
   */
  version: 1;
  /**
   * Declared credential slot NAMES. A slot never holds a value here; the host binds values in memory only.
   */
  credentialSlots: Array<SlotName>;
  candidates: Array<Candidate>;
  /**
   * Extensible intent tags. The seeded three are `reasoning`, `fast` and `cheap`; nothing in this schema or its consumers makes those spellings special.
   */
  capabilities: Array<Capability>;
  prompts: Array<ContentRef>;
  responseSchemas: Array<ContentRef>;
  components: Array<ComponentRef>;
  inference: Array<InferencePreset>;
  budgets: Array<BudgetPreset>;
  profiles: Array<RootProfile | ChildProfile>;
}


/**
 * Schema constraints this type cannot express: pattern="^[0-9a-f]{64}$"
 */
export type Sha2562 = string;

/**
 * Schema constraints this type cannot express: pattern="^[a-z0-9]+(-[a-z0-9]+)*$"
 */
export type Name2 = string;

/**
 * Schema constraints this type cannot express: pattern="^[a-zA-Z0-9_-]{1,64}$"
 */
export type ToolName2 = string;

/**
 * Schema constraints this type cannot express: pattern="^https?://[^@?#\\s]+$"
 */
export type SafeBaseUrl2 = string;

/**
 * Stable machine refusal/validation codes. TCFG1001 unknown capability tag; TCFG1002 unknown parent; TCFG1003 inheritance cycle; TCFG1004 duplicate id; TCFG1005 unresolvable reference; TCFG1006 role/candidate kind mismatch; TCFG1007 invalid document; TCFG1008 unavailable credential slot; TCFG1009 provider/model unavailable on host; TCFG1010 required feature unavailable; TCFG1011 missing required tool; TCFG1012 embedding identity disagreement; TCFG1013 credential-bearing URL; TCFG1014 secret-shaped member; TCFG1015 no available candidate for tag; TCFG1016 profile attempts to raise a host ceiling; TCFG1017 component unavailable at revision; TCFG1018 row references no identity; TCFG1019 registry revision mismatch; TCFG1020 invalid role override; TCFG1021 incomplete wire request.
 * Schema constraints this type cannot express: pattern="^TCFG1[0-9]{3}$"
 */
export type IssueCode = string;

export interface Issue {
  code: IssueCode;
  path: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  detail: string;
}


export interface RateCard2 {
  /**
   * Schema constraints this type cannot express: pattern="^[A-Z]{3}$"
   */
  currency: string;
  /**
   * Schema constraints this type cannot express: minimum=0
   */
  inputPerMillionTokens: number | null;
  /**
   * Schema constraints this type cannot express: minimum=0
   */
  outputPerMillionTokens: number | null;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  source: string;
  /**
   * Schema constraints this type cannot express: pattern="^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
   */
  asOf: string;
}


/**
 * Slot presence only. `source` names WHERE a value would come from (an environment variable name, `settings`), never the value.
 */
export interface CredentialSlotStatus {
  name: Name2;
  configured: boolean;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  source: string | null;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type HostManifestProvidersItemModelsOneOf1Item = string;

export interface HostManifestEmbeddingItem {
  provider: "openrouter" | "ollama" | "lmstudio" | "custom" | "builtin";
  base: SafeBaseUrl2 | null;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  model: string;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  dims: number | null;
}


export interface BudgetCeilings {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxCalls: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxTokens: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxMs: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxConcurrency: number | null;
}


export interface HostManifestObservationOneOf2 {
  kind: "declared" | "probed";
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  at: string | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  calls: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  failures: number;
}


/**
 * The credential-free host facts pure resolution consumes. Endpoints are suite-normalized bases; models/features are declared or probed; `observation` dates a LIVE refresh and stays null for synthetic/keyless manifests, which must be clock-free.
 */
export interface HostManifest {
  sourceClass: "desktop-settings" | "environment" | "synthetic" | "scripted";
  credentialSlots: Array<CredentialSlotStatus>;
  providers: Array<{ provider: "openrouter" | "ollama" | "lmstudio" | "custom"; base: SafeBaseUrl2; models: Array<HostManifestProvidersItemModelsOneOf1Item> | null; features: Array<Name2>; }>;
  embedding: Array<HostManifestEmbeddingItem>;
  /**
   * The compiled toolbox manifest — `createToolbox().list()` with each input schema's canonical revision.
   */
  tools: Array<{ name: ToolName2; description: string; inputSchemaRevision: Sha2562; }>;
  components: Array<{ id: Name2; revision: Sha2562; }>;
  budget: BudgetCeilings;
  observation: null | HostManifestObservationOneOf2;
}


/**
 * What a request may override per role — never a credential slot, host availability or a permission.
 */
export interface RoleOverride {
  capability?: Name2 | null;
  candidate?: Name2 | null;
  prompt?: Name2 | null;
  responseSchema?: Name2 | null;
  tools?: Array<ToolName2>;
  toolsRequired?: boolean;
  inference?: Name2 | null;
  ranker?: Name2 | null;
}


export interface InferenceControlsRetryOneOf2 {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  attempts: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  baseMs: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  maxMs: number;
}


export interface InferenceControlsReasoningOneOf2 {
  effort?: "none" | "minimal" | "low" | "medium" | "high";
  enabled?: boolean;
  exclude?: boolean;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxTokens?: number;
}


export interface InferenceControls {
  /**
   * Schema constraints this type cannot express: minimum=0, maximum=2
   */
  temperature: number | null;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxTokens: number | null;
  retry: null | InferenceControlsRetryOneOf2;
  reasoning: null | InferenceControlsReasoningOneOf2;
  maxTokensField?: "max_tokens" | "max_completion_tokens";
}


export interface LegacyWireStateOneOf2 {
  state: "configured";
  provider: "openrouter" | "ollama" | "lmstudio" | "custom";
  baseUrl: SafeBaseUrl2 | null;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  model: string;
  credentialSlot: Name2 | null;
  inference?: InferenceControls;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type LegacyWireStateOneOf3MissingItem = string;

export interface LegacyWireStateOneOf3 {
  state: "incomplete";
  requested: { provider?: string | null; baseUrl?: string | null; model?: string | null; };
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  missing: Array<LegacyWireStateOneOf3MissingItem>;
}


export interface LegacyWireStateOneOf4 {
  state: "configured-unproven";
  provider: "openrouter" | "ollama" | "lmstudio" | "custom";
  baseUrl: SafeBaseUrl2 | null;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  model: string;
  credentialSlot: Name2 | null;
}


/**
 * The generated projection of pre-profile desktop/environment settings for ONE wire. `unconfigured` is the only state eligible for built-in/grounded fallback; `incomplete` names what was requested and resolves to an issue, never to a silent substitute. `configured-unproven` records a wire that was configured but whose width no work of this run ever observed: the stack claims NO embedding identity rather than guessing one.
 */
export type LegacyWireState = { state: "unconfigured"; } | LegacyWireStateOneOf2 | LegacyWireStateOneOf3 | LegacyWireStateOneOf4;

/**
 * Schema constraints this type cannot express: propertyNames={"pattern":"^[a-z0-9]+(-[a-z0-9]+)*$"}
 */
export interface ProfileRequestOneOf1OverridesOneOf2 {
  [key: string]: RoleOverride;
}


/**
 * Schema constraints this type cannot express: propertyNames={"pattern":"^[a-z0-9]+(-[a-z0-9]+)*$"}
 */
export interface ProfileRequestOneOf2OverridesOneOf2 {
  [key: string]: RoleOverride;
}


export type ComponentRevision = null | { id: Name2; revision: Sha2562; };

export interface ContentRevision {
  id: Name2;
  revision: Sha2562;
}


export interface ProfileRequestOneOf3 {
  kind: "legacy";
  chat: LegacyWireState;
  embed: LegacyWireState;
  /**
   * The component references the host product genuinely runs for a legacy request — the shipped policy pipeline and ranker — recorded by the adapter that generated the projection, never guessed.
   */
  components: { policy: ComponentRevision; ranker: ComponentRevision; };
  /**
   * The versioned chat template the legacy host actually uses, as a content revision; null when no chat surface exists.
   */
  chatPrompt: ContentRevision | null;
}


/**
 * Exactly what was asked for: a named profile, a capability tag, or the generated legacy projection of pre-profile settings.
 */
export type ProfileRequest = { kind: "profile"; profile: Name2; overrides: null | ProfileRequestOneOf1OverridesOneOf2; } | { kind: "tag"; tag: Name2; overrides: null | ProfileRequestOneOf2OverridesOneOf2; } | ProfileRequestOneOf3;

/**
 * Requested set and the effective intersection with the host allowlist. A missing required tool refuses before an identity exists, so an identity's manifest never carries one.
 */
export interface ToolManifest {
  requested: Array<ToolName2>;
  effective: Array<{ name: ToolName2; inputSchemaRevision: Sha2562; }>;
}


/**
 * Everything that role actually runs with — the values, not the intent that selected them.
 */
export interface EffectiveRole {
  provider: "openrouter" | "ollama" | "lmstudio" | "custom";
  base: SafeBaseUrl2;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  model: string;
  credentialSlot: Name2 | null;
  inference: InferenceControls;
  prompt: ContentRevision | null;
  responseSchema: ContentRevision | null;
  tools: ToolManifest;
  rateCard: RateCard2 | null;
}


/**
 * The one embedding identity of the run — every corpus and query vector in the run uses it. Dims are the registry value CONFIRMED against a probe/reply, never guessed from a model label.
 */
export interface EffectiveEmbedding {
  provider: "openrouter" | "ollama" | "lmstudio" | "custom" | "builtin";
  base: SafeBaseUrl2 | null;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  model: string;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  dims: number;
  credentialSlot: Name2 | null;
}


/**
 * One complete, credential-free run identity: the exact request (profile, tag or generated legacy projection, plus role overrides) AND the complete effective stack — provider, suite-normalized base, model, inference controls, prompt and response-schema revisions, effective tool manifest, the one singleton embedding identity, policy/ranker component references, the effective budget and rate-card provenance. `identityId` is canonical SHA-256 over the identity's own payload excluding the id field; no clock, observation, usage, latency, key, secret or cache identifier is ever part of that payload. Reports and stores carry these in the `identityEnvelope` shape defined here: a table of identities and rows that either reference one (`run`), state that they are analysis that never ran a stack (`not-run`), or state that their facts were never recorded (`legacy-unrecorded`) — an absence is a stated absence, never a fabricated identity. Every object closes its properties, so a credential-shaped member cannot be carried.
 */
export interface RunIdentity {
  identityId: Sha2562;
  registryRevision: Sha2562 | null;
  hostManifestRevision: Sha2562;
  requested: ProfileRequest;
  /**
   * Schema constraints this type cannot express: propertyNames={"pattern":"^[a-z0-9]+(-[a-z0-9]+)*$"}
   */
  roles: { [key: string]: EffectiveRole; };
  /**
   * The one embedding identity of the run, or null when the stack resolved no embedding component (a legacy request whose configured wire was never proven by this run's work). A profile request always resolves one.
   */
  embedding: EffectiveEmbedding | null;
  components: { policy: ComponentRevision; ranker: ComponentRevision; };
  budget: BudgetCeilings;
}


export interface RowIdentityRefOneOf1 {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  rowId: string;
  identityStatus: "run";
  identityId: Sha2562;
}


export interface RowIdentityRefOneOf2 {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  rowId: string;
  identityStatus: "not-run" | "legacy-unrecorded";
}


/**
 * How a result row refers to the identity table. `run` rows name an identity that exists; `not-run` rows are analytic (a ceiling, a census) and cannot pretend to a provider; `legacy-unrecorded` rows keep historic results whose stack was never captured — honest absence, never a backfilled guess.
 */
export type RowIdentityRef = RowIdentityRefOneOf1 | RowIdentityRefOneOf2;

/**
 * The identity table plus the rows that refer to it — the shape every identity-bearing report and store carries. The `$query` assertions bind at validation time: identity ids are unique, row ids are unique, and every `run` row resolves to exactly one identity in the table.
 * Schema constraints this type cannot express: $query={"$and":[{"$eq":[{"$count":"$.identities[*].identityId"},{"$count":{"$distinct":"$.identities[*].identityId"}}]},{"$eq":[{"$count":"$.rows[*].rowId"},{"$count":{"$distinct":"$.rows[*].rowId"}}]},{"$every":{"r":"$.rows[?(@.identityStatus=='run')]"},"$satisfies":{"$exists":{"$index-of":["$.identities[*].identityId","$r.identityId"]}}}]}
 */
export interface IdentityEnvelope {
  identities: Array<RunIdentity>;
  rows: Array<RowIdentityRef>;
}


export interface ResolutionOneOf2 {
  ok: false;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  issues: Array<Issue>;
}


/**
 * The pure resolver's only two outcomes. Refusal is a value: sorted stable issues with document paths, no secret in any detail.
 */
export type Resolution = { ok: true; identity: RunIdentity; } | ResolutionOneOf2;
