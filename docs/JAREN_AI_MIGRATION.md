# Jaren AI ownership and qualification

Jaren supplies independent generic engines, contracts and complete editors.
Tangle owns the model, context and agent mechanisms, their Jaren integrations,
the reusable assistant, and the Pages adventure. The source close-out uses
Jaren 0.84.0 and Tangle 0.21.0. The [close-out receipt](migrations/jaren-ai/closeout.json)
records the committed foundation, release inputs, commands and limits. Registry
publication remains separate from this source close-out.

## Source and installation identity

| Input | Identity |
|---|---|
| Jaren source release | `b111a633f6d35f00109902a16cc25776ccaaf4ea`, version `0.84.0` |
| Foundation source state | Committed; empty patch SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| Tangle source release | `0.21.0`; the release commit carries [the prepared release record](../releases/0.21.0.json) |
| Source provenance | Original execution bases remain recoverable; the Jaren gitlink now names the committed AI-free source |
| Foundation mode | 23 complete AI-free Jaren tarballs; exact relative `file:dist/jaren/*.tgz` root dependencies and matching lock integrity |
| Toolchains | Jaren Node `24.20.0`, Bun `1.4.2`; Tangle Node `24.19.0`, Bun `1.4.0`; npm `11.12.1` |

The [foundation manifest](migrations/jaren-ai/foundations.json) records each
archive's SHA-256, npm integrity, complete member hashes and dependency edges.
The current source patch is empty: bootstrap builds the exact committed
gitlink in a disposable checkout. A committed source receipt cannot carry an
uncommitted patch. Capture cleans generated output
before building: declaration emitters otherwise leave files for removed modules.
No installed package is patched, repaired, source-linked or silently substituted.
The verifier rejects missing/altered archives, extra installed files, unrecorded
or nested legacy foundations, unsafe member paths, changed edges and wrong pins.
A missing archive never permits bootstrap to overwrite a different existing one.

The earlier 0.83.3 candidate is preserved in [its manifest](migrations/jaren-ai/candidate-0.83.3.json)
and [patch](migrations/jaren-ai/candidate-0.83.3.patch), with the
[pre-closeout verification](migrations/jaren-ai/verification.json). Those dated
results describe the original Jaren `3491513e` / Tangle `13330f3` execution bases;
they are not registry-release claims.

Tangle's existing release tooling emits 13 public JavaScript/declaration packages.
`dist/release/artifacts.json` records the exact source input fingerprint and
package integrities; `dist/release/verification.json` binds the successful
external consumers to those artifacts. Both are generated by the documented
commands. A subsequent source edit invalidates that verification. Their source
fingerprint includes the tracked receipts and this document, so this document
does not embed a circular hash of itself.

## Public ownership

| Previous concern | Current public owner |
|---|---|
| Chat, provider routing, streaming, embeddings, replay, structured generation | `@tangleai/models` and its named subpaths |
| Ledger, bounded environment, evidence, recall, retention and storage | `@tangleai/context`; browser slot adapter at `/storage/slot` |
| Toolbox, bounded agents, program execution, recursion and refinement | `@tangleai/agents` and its named subpaths |
| Stylesheet/spatial authors and geo tools | `@tangleai/jaren/stylesheet`, `/spatial`, `/geo-tools` |
| Studio file author and complete editor AI adapters | `@tangleai/jaren/studio`, `/data`, `/flow` |
| Typed AI program pen formerly at `@jarenjs/linq/ai` | `@tangleai/jaren/program` |
| Assistant state/actions/controller and visual mount | `@tangleai/assistant`, `/component`, scoped exported CSS |
| Adventure content, forms, scene state, save/load and optional NPC dialogue | Private `apps/pages/src/demos/game` |
| Strict checks and generic guarded editing | `@jarenjs/core/check`, `/guarded` |
| Generic document building and captured expressions | `@jarenjs/linq/authoring` |
| Complete project, Data and Flow editors | `@jarenjs/studio/component`, `/data`, `/flow`, `/data/host`, exported contracts/styles |
| Browser registration of ordinary validated operations | `@jarenjs/contract/webmcp` |

There is no compatibility AI package or AI forwarding subpath in Jaren. No
Jaren workspace, private app, development dependency or active source entry
consumes Tangle. Tangle's models/context/agents do not depend on the higher
policy packages. Core's ledger projection depends on context; existing config,
memory, pipeline, documents, store and MAS retain their responsibilities.
Migrated JS/JSDoc and JS tests retain their source language. All emitted public
declarations are checked strictly, including external consumers with
`skipLibCheck: false`; installed code is JavaScript on Node and Bun.

## Source, assertion and evidence receipts

The [transfer manifest](migrations/jaren-ai/manifest.json) accounts for all 37
original source modules, 25 code entry points plus metadata, 117 runtime root
symbols, and 25 declaration surfaces containing 264 symbol occurrences.
The runtime allocation is 114 symbols in Tangle and three generic Jaren
symbols; the declaration allocation is 260 Tangle occurrences and four Jaren
occurrences. The former barrel is split explicitly, with no placeholder owner.

All 44 original AI test files have receiving homes. The
[assertion audit](migrations/jaren-ai/assertions.json) distinguishes the original
585 executed tests from 548 literal case names: 532 names survive in their
receiving files, seven generic cases remain in Jaren, and nine dependency,
documentation or asynchronous browser-result cases have explicit adaptations.
The [removal receipt](migrations/jaren-ai/removal.json) covers 167 removed paths
and the mixed-test split: 238 retained literal names, 22 received/replaced names
and four new names across 23 files, with parameterized documentation and browser
assertions recorded separately. These counts are audit aids, not substitutes
for runtime test results or coverage.

Twenty-six shared-editor source sections remain Jaren-owned. The original
[experience receipt](migrations/jaren-ai/experiences.json) preserves the exact
pre-removal qualification of the 13 dedicated assistant/game surfaces and
mixed host sections. Current destination hashes are in the main manifest;
subsequent qualification supersedes earlier artifact identities without
rewriting their historical receipts. The additional
[manual-tool receipt](migrations/jaren-ai/manual-tools.json) covers the live
assistant driver and configured screenshots. The driver also supports
`SCRIPTED=1`: two real browser turns completed with two intercepted requests,
no console errors and no paid call. Four original desktop/phone viewports,
including 320 pixels, render the configured assistant.

The [measurement receipt](migrations/jaren-ai/measurements.json) keeps original
inputs, hashes, historical versions, paid failures and thresholds. Generic
query/DB/vector/grammar instruments stay in Jaren. AI-only instruments, their
helper closure and derived facts now live in Tangle. New keyless results are
under `benchmark/migration/`; historical paid artifacts were not relabeled.
The program pen measures 15,678 bytes in Bun 1.4.0, with nine retained modules
and zero excluded runtime-engine bytes. Its external bundle gate enforces the
18,000-byte ceiling. The original 15,472-byte esbuild observation remains a
separate historical measurement.

History uses a source snapshot with immutable provenance, because modules and
mixed editor sections split across owners. No existing refs were rewritten.
The prepared snapshot SHA-256 is
`d6c2c095121ba018a957dd9027683fe7cc64be7eb9dc51bb8383b8ec4a4f189d`.
Every original file can be recovered with `git show <commit>:<path>` from the
Jaren repository and checked against its recorded hash. There is no claim that
filtered history was merged into Tangle.

## Editors, assistant and browser protocol

Both sites mount one complete Jaren-owned Studio/Data/Flow implementation.
Tangle owns thin routes, seeds, downloads, storage identities and worker
initialization. Its authors validate and publish through the same public
revision checks as manual edits. Invalid or stale candidates preserve human
work. Project runs return settled host results. State-only app updates preserve
the mounted stage, focus and selection; structural edits advance its stage
revision. Data keeps unfinished buffers across deactivation and resumes through
its owned worker lifecycle. Real SQLite storage, migrations, independent query
replies, live streams, startup failure/retry and late disposal are exercised.

The private Pages app pins `@sqlite.org/sqlite-wasm@3.53.0-build1`, verifies its
real installation and emits two worker entries, WASM and the OPFS proxy. This
initializer exception never enters a published package. Jaren's public driver,
contract, transport, controllers and scoped styles remain the implementation.
See [Pages host requirements](migrations/jaren-ai/PAGES.md).

Assistant mounts own their listeners, transcript, requests and writer lifecycle.
Cancel/dispose fences late provider replies, tool calls and child authors.
Storage failures remain visible. Game exit cancels pending NPC dialogue;
scripted play, victory, save/load, localization and no-key/provider-failure
paths remain available. Optional tools without host services return explicit
refusals. Jaren exposes only its ordinary site/editor operations through plain
contracts, without an assistant or model configuration.

Both hosts use the same `@jarenjs/contract/webmcp` adapter. Automatic discovery
chooses a usable `document.modelContext`, then `navigator.modelContext`;
getters are guarded and an alias registers once. An own `options.context`,
including `null` or `undefined`, is authoritative. The Tangle wrapper's legacy
positional `undefined` means automatic discovery. Supported methods are current
`registerTool` and legacy `provideContext`, independently of which root exposes
them. Await `binding.ready`; a pending registration is never reported as success.
Refusal or partial mutation does not trigger fallback into a second root.
Disposal owns rollback and callback deactivation, including pending and late
registration. Without a native removal API, the result reports that limitation;
foreign registrations are never cleared. Explicit refresh/focus can detect
later availability. Back/forward-cache suspension preserves ownership, and
final page departure releases it.

Stock headless Chromium `149.0.7827.55`, Firefox `151.0` and WebKit `26.5` on
localhost, without feature flags or an origin trial, expose neither root.
Native integration is therefore **unavailable**, separately from the passing
injected compatibility matrix and ordinary UI checks. Unknown future protocol
changes are not claimed compatible: requalify discovery, methods, asynchronous
results, partial refusal and cleanup with both-root tests before supporting one.

## Persisted data and checkpoint limits

Existing schema IDs, replay/cache bytes, embedding identities, budgets and
ordinary ledger/memory formats remain stable. Storage is origin-scoped. The
[explicit transfer procedure](migrations/jaren-ai/BROWSER_DATA.md) exports
selected non-secret transcript, ledger, game, project and experiment slots from
the old origin and imports only into empty or identical destinations. It
preflights conflicts, reports partial writes, and repeats without writes.
Credentials and provider settings are excluded. No automatic cross-origin
access, credential migration or global transaction across localStorage writes
is promised. Pages conservatively uses one ledger writer on every browser.

MAS executable identity is
`tangle-mas/3:flow/<version>:models/<version>:context/<version>:agents/<version>:<registry>`.
Legacy executable checkpoints are refused with `TMAS2002` before effects or
writes; repeating the attempt leaves the checkpoint intact. Ordinary records
remain readable. No automatic legacy executable conversion is claimed.

## Verification commands and measured gates

Initialize the source submodule, use the foundation manifest's toolchain and
bootstrap **before** dependency installation:

```sh
git submodule update --init vendor/jarenjs
# Node 24.20.0, npm 11.12.1; this command has no installed dependencies:
node scripts/jaren-artifacts.ts --bootstrap
# Restore Tangle Node 24.19.0, npm 11.12.1 and Bun 1.4.0:
npm ci --ignore-scripts
npm run check
npm run release:build
npm run release:test
npm run pages:build
npm run pages:test:browser
```

Run declaration/build commands sequentially in a checkout: they own the same
generated declaration directories. CI's composite setup performs the bootstrap
under the recorded Jaren pins, then restores its requested Tangle runtime.
The Windows and minimum-Node jobs use that ordering too. A fresh source
reconstruction reproduced all 23 archives; a second bootstrap preserved all
archive mtimes, and two clean installs verified every byte and lock edge.
The initial independent Tangle gate passed all 1,377 tests after generating its
optional local LoCoMo oracle with the existing Python instrument. Its 4,602
dataset-derived rows remain ignored and are never redistributed.

| Measured gate | Result |
|---|---|
| Jaren full source/site gate, including independent checkout with six test submodules and no Tangle sibling | 14,263 tests: 14,222 pass, 41 existing skips; 2,243 suites; 8,261/8,261 functions; zero dead files/functions and lint findings |
| Jaren browser matrix | 409 pass, 5 platform skips across Chromium/Firefox/WebKit |
| Jaren independent distribution and source gates | Strict types, 23 packed packages, tree shaking, dependencies, portable lock, native tooling and source consumers pass |
| Tangle 0.21.0 full source gate | 1,379 pass, 297 suites, no skips; 53 strictly checked JS declaration modules |
| Tangle Pages browser matrix | 118 pass, 8 declared skips: three paid-provider cases and five unavailable WebKit OPFS cases |
| Existing Pages and desktop | Landing pipeline/mobile check passes; source Chromium and compiled-binary WebKit pass with grounded offline citations; binary runs from a foreign directory |
| Config and MAS | Config 44/44; MAS seven substrate probes, six durability probes, 11 runtime oracles and seven exact refusals |
| External Tangle distributions | 13 JavaScript/declaration packages; 103 exports on Node/Bun; 260 mapped type occurrences; five editor and six assistant cases per runtime; strict types and browser bundles pass; minimum Node 24.0.0 also passes |

The Jaren dependency audit accepts 24 existing advisories confined to benchmark
rivals/build tooling; none reach shipped packages. These are declared existing
exceptions, not newly introduced runtime dependencies. Native-browser and
paid-provider skips are not counted as successful integrations.

## Scoped quirk audit and release state

Confirmed migration defects, with reproductions and fixes:

- Document-only browser discovery, asynchronous readiness and ownership were
  incomplete; the shared adapter and both hosts now retain the full matrix.
- Public editor publication could lose raw Data input or restart state-only
  Studio changes; real browser and public-operation regressions retain buffers,
  stage identity, focus and results.
- Assistant cancellation and closed storage ownership could accept late work;
  controller/storage tests and browser cases now fence it and surface failures.
- A persisted `pagehide` consumed final cleanup; the three-engine reproduction
  now retains the handler until final departure.
- Dirty capture packed a deleted Studio author declaration; clean reconstruction
  refused the archive, and capture now cleans before emitting declarations.
- Artifact verification admitted extra files/edges and bootstrap checked pins
  too late; focused negative fixtures now refuse these inputs without repair.
- CI installed before artifact bootstrap, Pages metadata described candidate
  input as registry input, and publication lacked an explicit candidate-mode
  refusal; setup, metadata and publication now enforce the recorded mode.
- Manual assistant tooling remained in Jaren; it is received in Tangle with
  scripted browser and four-viewport evidence.

Confirmed adjacent findings and dispositions:

- Existing benchmark-only dependency advisories remain under Jaren's documented
  dependency audit policy; no runtime dependency was added to address them.
- Historical live authoring, depth and retrieval losses remain measured losses.
  Their open obligations belong to [the AI mechanism roadmap](AI-MECHANISM-ROADMAP.md).
- Native WebMCP and WebKit OPFS availability are tested platform limitations.
  Provider configuration and a future protocol qualification remain host work.

Checked and dropped suspicions:

- The pen's error import might retain a query/runtime engine. The measured
  isolated bundle retains nine authoring modules and zero excluded engine bytes;
  no new foundation API was invented for that suspicion.
- Missing declarations during one concurrent build were caused by two commands
  replacing the same generated directories. Sequential full gates preserve
  strict checks; no declaration stub or skipped library check was introduced.
- Changed MAS report hashes might indicate a functional regression. Regenerated
  identity/handoff reports preserve all 11 positive and seven negative oracles.
- Contract projection, export ZIPs, vectors and generic grammar profiles are
  independent of AI and remain in Jaren with their assertions.
- Generic editor duplication, silent storage failures, package cycles, private
  runtime imports, orphan worker/styles and unexecuted inherited JS tests were
  checked through source, operation, browser and packed-consumer gates.

Jaren's AI-free source is committed at `b111a633f6d35f00109902a16cc25776ccaaf4ea` for 0.84.0,
and Tangle's gitlink names that revision. The five introduced packages join the
existing fixed Changesets group in 0.21.0; the tracked release record identifies
its source commit and the generated full gate binds the exact input fingerprint
and package artifacts. The original repository history was not rewritten or
merged through a filtered history.

The Jaren close-out protocol deploys its website, tags and pushes after its
committed gate passes. Tangle's close-out command commits locally; its push and
deployment remain separate. Neither operation establishes npm availability.
Publication preflight still refuses candidate artifact mode, including artifacts
from committed source. A registry release must publish and verify the AI-free
Jaren closure first, switch Tangle's exact dependencies and lock to that registry
release, then repeat release verification and publication availability checks.
Pages remains independent of Tangle npm publication. POLICY, later Tangle
campaigns and generic Jaren roadmap work remain parked.
