# MAS runtime conformance — the registered instrument and its baseline

Generated by `npm run benchmark:mas` from the committed fixtures; do not edit numbers by hand.

Registered: **11 positive** workflow fixtures with exact output/event/aggregation/concurrency/call oracles and **7 negative** fixtures with reserved refusal codes and pointers (manifest `ceee5f7eefd6…`, fixture registry `197bf5f77c4d…`). The registration is immutable input: later measurements replace integrated rows by id and never change these denominators.

## Suite substrate probes

Direct executions of the published JarenJS primitives this campaign lowers to, with controlled promises and injected clocks. A probe passing says the substrate exists; it does not pass a MAS fixture.

| probe | outcome | observed |
|---|---|---|
| `dag-fanout-concurrency` | pass | three gated handlers were in flight together (entry/exit counter reached exactly 3) before any settled |
| `dag-fanin-edge-order` | pass | settling c, b, a still delivered the port object as a, b, c — completion order cannot change a value |
| `dag-abort-siblings` | pass | the scripted failure rejected as JF2006 naming its node; the gated sibling observed the shared abort signal and recorded aborted |
| `dag-checkpoint-restore` | pass | the resumed run seeded the checkpointed value, fired a restored record and made zero duplicate handler calls |
| `fsm-document-order-resume` | pass | two matching transitions resolved to the first in document order; the snapshot string resumed the session to its final state |
| `db-job-reclaim-checkpoints` | pass | idempotent enqueue kept one row; the injected clock expired the lease; the reclaim read the saved checkpoint and its completion atomically pruned the rows |
| `budget-concurrent-reserve` | pass | three concurrent reservations consumed the cap synchronously and the fourth stopped as budget-turns before any call launched |

## Durable persistence measurements

The real MAS store adapter and suite queue under injected clocks — durable-scripted evidence, never a runtime fixture pass.

| probe | outcome | observed |
|---|---|---|
| `atomic-node-completion` | pass | an injected failure after each of the five stages left no partial member; the successful commit wrote all five together |
| `activation-cas` | pass | one compare-and-swap transition applied; nineteen refused TMAS2001 against the moved head |
| `semantic-idempotency` | pass | the replay returned the stored completion with zero new rows; a different payload under the key refused TMAS2001 |
| `segment-checkpoint-namespace` | pass | each namespace saw only its own rows through the delegated suite store, and the terminal completion marked the job done and pruned the segment in one guarded statement |
| `crash-reclaim-closes` | pass | the reclaimed handler read the committed segment outcome and closed the job without invoking the executor again |
| `uncertain-guard` | pass | the reclaim answered TMAS2006 uncertain instead of silently buying or applying again; resolution is the operator's |

## Integrated MAS baseline

11/11 positive runtime oracles pass; 7/7 negative fixtures refuse at their registered code and pointer; 0 validate without executing; 0 are not implemented.

| fixture | family | state | calls | tool calls | context reads | restores | max concurrency | refusal |
|---|---|---|---:|---:|---:|---:|---:|---|
| `sequential` | positive | runtime-pass | 0 | 0 | 0 | 0 | 1 | — |
| `fanout-fanin` | positive | runtime-pass | 0 | 0 | 0 | 0 | 3 | — |
| `message-order` | positive | runtime-pass | 0 | 0 | 0 | 0 | 3 | — |
| `switch-one` | positive | runtime-pass | 0 | 0 | 0 | 0 | 1 | — |
| `switch-many` | positive | runtime-pass | 0 | 0 | 0 | 0 | 2 | — |
| `loop-three` | positive | runtime-pass | 0 | 0 | 0 | 0 | 1 | — |
| `nested-state` | positive | runtime-pass | 0 | 0 | 0 | 0 | 1 | — |
| `interaction` | positive | runtime-pass | 0 | 0 | 0 | 0 | 1 | — |
| `checkpoint` | positive | runtime-pass | 1 | 0 | 0 | 1 | 1 | — |
| `failure-abort` | positive | runtime-pass | 0 | 0 | 0 | 0 | 2 | — |
| `weekly-report-manual` | positive | runtime-pass | 11 | 3 | 1 | 0 | 3 | — |
| `plain-cycle` | negative | refused-as-registered | 0 | 0 | 0 | 0 | — | `TMAS1005` at `/messages/2` |
| `unknown-port` | negative | refused-as-registered | 0 | 0 | 0 | 0 | — | `TMAS1004` at `/messages/1/to/port` |
| `switch-no-default` | negative | refused-as-registered | 0 | 0 | 0 | 0 | — | `TMAS1006` at `/nodes/0/default` |
| `unbounded-loop` | negative | refused-as-registered | 0 | 0 | 0 | 0 | — | `TMAS1007` at `/nodes/0/maxIterations` |
| `undeclared-tool` | negative | refused-as-registered | 0 | 0 | 0 | 0 | — | `TMAS1009` at `/nodes/0/tools/0` |
| `child-over-budget` | negative | refused-as-registered | 0 | 0 | 0 | 0 | — | `TMAS1008` at `/nodes/0/limits/calls` |
| `incompatible-wire` | negative | refused-as-registered | 0 | 0 | 0 | 0 | — | `TMAS1004` at `/messages/0/to` |

Scripted totals across integrated rows: 12 model calls, 3 tool calls, 1 context reads, 1 restores. The keyless gate is structural: transport calls and live model calls are literal zeros in the schema.

## Mechanical decision

**runtime-conformant** — all seven clauses recomputed by the report schema at validation time.

| clause | state |
|---|---|
| registration | holds |
| conformance | holds |
| concurrencyOrder | holds |
| durability | holds |
| controlSafety | holds |
| persistenceIdentity | holds |
| hygiene | holds |

Live weekly-report row: **not-run** — no authorized live plan. Runtime conformance does not depend on a live model: the claims are scheduler/contract/durability semantics and the scripted clients exercise the exact AI/tool/context seams.

Report `a611e57c191e…` at suite @jarenjs/flow@0.91.3; flat baseline projected by `queries/mas/runtime-baseline.json`.
