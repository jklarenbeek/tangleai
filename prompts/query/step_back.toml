[meta]
name = "step_back"
version = "1.0"
description = "Query step_back technique"
module = "QueryTranslator"

[config]
temperature = 0.3
max_tokens = 512

[[messages]]
role = "user"
content = """
What is the broader topic or principle behind this question: "{{query}}"? Respond with one broader question."""

