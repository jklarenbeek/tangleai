/**
 * Embedded UI assets — THIS COMMITTED VERSION IS THE EMPTY STUB.
 *
 * In dev the static layer serves `public/` from disk and this map is
 * never consulted. `npm run desktop:compile` overwrites it (via
 * scripts/embed-assets.ts) so the compiled binary carries the frontend,
 * then restores the stub — a populated version of this file must never
 * be committed.
 */
export const ASSETS: Record<string, string> = {};
