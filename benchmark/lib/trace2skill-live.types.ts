// Generated by @jarenjs/emit from benchmark/schemas/trace2skill-live.schema.json.
// Do not edit: regenerate instead.

/**
 * Schema constraints this type cannot express: pattern="^[0-9a-f]{64}$"
 */
export type Sha256 = string;

/**
 * One measured row as a live tier would rerun it: the calls the keyless tier actually made are the ceiling a live rerun is planned against, because the wire answers the same registered units.
 */
export interface LivePlanRow {
  /**
   * Schema constraints this type cannot express: pattern="^(deepening|creation):[a-z][a-z0-9-]*$"
   */
  id: string;
  /**
   * Schema constraints this type cannot express: pattern="^[a-z][a-z0-9-]*$"
   */
  condition: string;
  status: "run" | "not-run" | "implementation-missing";
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  units: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  maxFreshCalls: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  cacheHits: number;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type LivePlanModelOneOf1 = string;

/**
 * Schema constraints this type cannot express: minLength=1
 */
export type LivePlanBaseUrlOneOf1 = string;

/**
 * Schema constraints this type cannot express: minLength=1
 */
export type LivePlanPromptVersionsItemItem = string;

/**
 * Schema constraints this type cannot express: minItems=2, maxItems=2
 */
export type LivePlanPromptVersionsItem = Array<LivePlanPromptVersionsItemItem>;

/**
 * Schema constraints this type cannot express: pattern="^[a-z][a-z0-9-]*$"
 */
export type LivePlanStagesItem = string;

/**
 * Schema constraints this type cannot express: minLength=1
 */
export type LivePlanSkippedOneOf1 = string;

/**
 * What an authorized run would do, hashed into `planId`. Everything in it is credential-free: a provider, a model name, a base URL without userinfo, the NAME of the key variable, the ceilings and the rows.
 * Schema constraints this type cannot express: $query={"$and":[{"$eq":["$.authorized",false]},{"$eq":["$.maxFreshTotal",{"$sum":"$.rows[*].maxFreshCalls"}]},{"$eq":["$.cacheHits",{"$sum":"$.rows[*].cacheHits"}]}]}
 */
export interface LivePlan {
  planId: Sha256;
  authorized: false;
  instrument: "trace2skill";
  mode: "deepening" | "creation";
  fixtureId: Sha256;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  provider: string;
  model: LivePlanModelOneOf1 | null;
  baseUrl: LivePlanBaseUrlOneOf1 | null;
  /**
   * The NAME of the environment variable a key would be read from. Never a key.
   * Schema constraints this type cannot express: minLength=1
   */
  keySource: string;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxCalls: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  maxConcurrency: number;
  promptVersions: Array<LivePlanPromptVersionsItem>;
  stages: Array<LivePlanStagesItem>;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  rows: Array<LivePlanRow>;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  maxFreshTotal: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  cacheHits: number;
  skipped: LivePlanSkippedOneOf1 | null;
}


/**
 * Schema constraints this type cannot express: $query={"$and":[{"$eq":["$.calls",0]},{"$eq":["$.spend.calls",0]},{"$eq":["$.spend.tokens",0]}]}
 */
export interface Trace2skillLive {
  instrument: "trace2skill-live";
  version: 1;
  status: "not-run";
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  reason: string;
  /**
   * Provider requests this record accounts for. Zero, by construction: a network trap counts every attempt and no attempt is made.
   */
  calls: 0;
  spend: { calls: 0; tokens: 0; };
  plan: LivePlan;
  recordId: Sha256;
}


/**
 * The frozen, credential-free description of what a live skill-evolution tier WOULD spend, and the record that it was not spent. A key never reaches this document: it carries the NAME of the variable a key would be read from and nothing else. `status` is `not-run` in every committed copy, because executing the plan needs an explicit operator approval that names its `planId`.
 */
export type Trace2skillLive2 = Trace2skillLive;

