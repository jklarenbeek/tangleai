// Generated by @jarenjs/emit from benchmark/schemas/evolve.schema.json.
// Do not edit: regenerate instead.

/**
 * Schema constraints this type cannot express: pattern="^[0-9a-f]{64}$"
 */
export type Sha256 = string;

/**
 * Schema constraints this type cannot express: pattern="^[0-9a-f]{40}$"
 */
export type GitRevision = string;

export type Decision = "kept" | "abandoned" | "refused" | "uncertain";

export type Reason = "improved" | "equal" | "regression" | "red" | "ambiguous" | "over-budget" | "unverifiable" | "goalpost" | "escape" | "command" | "uncertain-effect";

export type Code = "TEVO1001" | "TEVO1002" | "TEVO1003" | "TEVO1004" | "TEVO1005" | "TEVO1006" | "TEVO1007" | "TEVO1008" | "TEVO1009" | "TEVO1010" | "TEVO1011" | null;

/**
 * Every reason in the vocabulary, always present. A reason that did not happen is a zero, never a missing key: an absent column is how a losing row disappears.
 */
export interface ReasonCounts {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  improved: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  equal: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  regression: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  red: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  ambiguous: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  "over-budget": number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  unverifiable: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  goalpost: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  escape: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  command: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  "uncertain-effect": number;
}


/**
 * What the registration says one proposal must produce. Written before any mechanism exists, and never edited to match a measurement.
 */
export interface Expectation {
  decision: Decision;
  reason: Reason;
  code: Code;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type StrategyToolsItem = string;

/**
 * A hand-authored strategy, shaped as the ledger's skill record plus the evidence that carries it. The ledger assigns `at` and any embedding when the record is inserted; a registration holds no clock.
 */
export interface Strategy {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  id: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  name: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  when: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  instructions: string;
  tools: Array<StrategyToolsItem>;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  evidence: string;
}


/**
 * One RFC 6902 operation over `{ files: { <relative path>: <utf-8 text> } }`. Nothing outside `/files/` is addressable, and the pointer is the only place a path is stated.
 */
export interface PatchOperation {
  op: "add" | "replace" | "remove";
  /**
   * Schema constraints this type cannot express: pattern="^/files/"
   */
  path: string;
  value?: string;
}


/**
 * A hand-authored candidate change and the decision the registration demands of it. Model-authored proposals are a separate, later, explicitly approved mode; nothing here is model output.
 */
export interface Proposal {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  id: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  strategyId: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  rationale: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  evidence: string;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  patch: Array<PatchOperation>;
  expect: Expectation;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type CommandCommandItem = string;

/**
 * A command as an argv array, beside the fixture package script that spells it. No shell string is ever registered, and the two must agree.
 */
export interface Command {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  script: string;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  command: Array<CommandCommandItem>;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type PolicyImmutablePathsItem = string;

/**
 * Schema constraints this type cannot express: minLength=1
 */
export type PolicyImmutablePrefixesItem = string;

export interface PolicyImmutableGeneratedItem {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  path: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  generator: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  source: string;
}


/**
 * What a proposal may not reach: tests and their fixtures, the gate script, CI, the fitness instrument, the registered truth and thresholds, and every generated file together with the generator that owns it.
 */
export interface PolicyImmutable {
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  paths: Array<PolicyImmutablePathsItem>;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  prefixes: Array<PolicyImmutablePrefixesItem>;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  generated: Array<PolicyImmutableGeneratedItem>;
}


/**
 * The registered base value of the fitness metric. A measured base that differs from it makes the experiment unverifiable rather than an improvement.
 */
export interface PolicyTruth {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  path: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  metric: string;
  /**
   * Schema constraints this type cannot express: type="integer"
   */
  value: number;
}


/**
 * Which way the metric must move and by how much. Registered here, outside the worktree, so no patch can reach it.
 */
export interface PolicyThresholds {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  path: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  metric: string;
  direction: "lower" | "higher";
  minDelta: number;
}


/**
 * The immutable surface and the registered commands, held outside the repository under experiment so a proposal cannot move a goalpost by editing the file that defines it.
 */
export interface Policy {
  /**
   * What a proposal may not reach: tests and their fixtures, the gate script, CI, the fitness instrument, the registered truth and thresholds, and every generated file together with the generator that owns it.
   */
  immutable: PolicyImmutable;
  gate: Command;
  instrument: Command;
  /**
   * The registered base value of the fitness metric. A measured base that differs from it makes the experiment unverifiable rather than an improvement.
   */
  truth: PolicyTruth;
  /**
   * Which way the metric must move and by how much. Registered here, outside the worktree, so no patch can reach it.
   */
  thresholds: PolicyThresholds;
  /**
   * The contract the fitness command's stdout must satisfy. Unparseable or schema-invalid output is a refused measurement, never a zero.
   */
  metricSchema: { [key: string]: unknown; };
}


/**
 * Every bound an experiment runs under. Exhaustion abandons and is counted; nothing here may be raised from inside an experiment.
 */
export interface Budgets {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  patchBytes: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  patchFiles: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  gateTimeoutMs: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  fitnessTimeoutMs: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  stdoutBytes: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  stderrBytes: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  workspaceBytes: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  samples: number;
}


export interface SourceManifestFilesItem {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  path: string;
  sha256: Sha256;
}


/**
 * The ordered path/digest manifest of everything this instrument's behavior reads. Commit HEAD is deliberately absent: committing identical bytes does not invalidate a measurement.
 */
export interface SourceManifest {
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  files: Array<SourceManifestFilesItem>;
  sha256: Sha256;
}


export interface ManifestFixture {
  id: "evolve-fixture-rank/v1";
  licence: "MIT";
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  provenance: string;
  baseRevision: GitRevision;
  repo: "benchmark/fixtures/evolve/repo";
}


export interface ManifestFilesItem {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  path: string;
  sha256: Sha256;
}


export interface ManifestProposalsItem {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  id: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  path: string;
  revision: Sha256;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  strategyId: string;
  expect: Expectation;
}


/**
 * The registration: the fixture repository's identity and file digests, the immutable-surface policy, the budgets, the strategy library revision and every proposal with the decision it must produce.
 */
export interface Manifest {
  manifest: "evolve-fixture";
  version: 1;
  fixture: ManifestFixture;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  files: Array<ManifestFilesItem>;
  policy: Policy;
  budgets: Budgets;
  strategiesRevision: Sha256;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  proposals: Array<ManifestProposalsItem>;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  hostProbes: Array<"symlink-indirection" | "protected-ref-write" | "unknown-command" | "env-leak">;
}


/**
 * What the proposal costs before anything runs: the UTF-8 size of its canonical patch and the number of files it touches, each against the registered cap.
 */
export interface RowBudgets {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  patchBytes: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  patchFiles: number;
  withinPatchBytes: boolean;
  withinPatchFiles: boolean;
}


/**
 * Fenced effect legs this row spawned, and how many settled unresolved. An unresolved leg stops for reconciliation; it is never retried into a number.
 */
export interface RowEffects {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  legs: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  unresolved: number;
}


/**
 * One registered proposal's row. `implementation-missing` is the honest state while no executor exists: it carries no actual decision and claims no match.
 */
export interface Row {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  proposalId: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  strategyId: string;
  state: "implementation-missing" | "run";
  actual: { decision: Decision; reason: Reason; code: Code; } | null;
  matches: boolean | null;
  /**
   * What the proposal costs before anything runs: the UTF-8 size of its canonical patch and the number of files it touches, each against the registered cap.
   */
  budgets: RowBudgets;
  /**
   * Fenced effect legs this row spawned, and how many settled unresolved. An unresolved leg stops for reconciliation; it is never retried into a number.
   */
  effects: RowEffects;
}


/**
 * A refusal that cannot be expressed as a file-map patch and must be proven against the host instead.
 */
export interface HostProbe {
  id: "symlink-indirection" | "protected-ref-write" | "unknown-command" | "env-leak";
  state: "implementation-missing" | "pass" | "fail";
  detail: string | null;
}


export interface StrategyControl {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  strategyId: string;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  attempted: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  kept: number;
  /**
   * Schema constraints this type cannot express: minimum=0, maximum=1
   */
  hitRate: number;
}


/**
 * The registration read back as a scorer: what the hit rate is if every proposal produces exactly the decision registered for it. It is a constant a mechanism must reproduce, never a quality claim.
 */
export interface ControlsOracle {
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  strategies: Array<StrategyControl>;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  attempted: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  kept: number;
  /**
   * Schema constraints this type cannot express: minimum=0, maximum=1
   */
  hitRate: number;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type ControlsRandomSelectedItem = string;

/**
 * A seeded shuffle picking proposals with no strategy at all. Ranked selection that cannot beat this row has not earned its cost.
 */
export interface ControlsRandom {
  /**
   * Schema constraints this type cannot express: type="integer"
   */
  seed: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  selects: number;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  selected: Array<ControlsRandomSelectedItem>;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  attempted: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  kept: number;
  /**
   * Schema constraints this type cannot express: minimum=0, maximum=1
   */
  hitRate: number;
}


/**
 * The two rows that license every other number: an exact oracle and a seeded random draw.
 */
export interface Controls {
  /**
   * The registration read back as a scorer: what the hit rate is if every proposal produces exactly the decision registered for it. It is a constant a mechanism must reproduce, never a quality claim.
   */
  oracle: ControlsOracle;
  /**
   * A seeded shuffle picking proposals with no strategy at all. Ranked selection that cannot beat this row has not earned its cost.
   */
  random: ControlsRandom;
}


export interface CountsAbandoned {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  total: number;
  byReason: ReasonCounts;
}


export interface CountsRefused {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  total: number;
  byReason: ReasonCounts;
}


/**
 * The census over the rows. `protectedRefWrites` and `liveModelCalls` are literal zeros the schema asserts: a report of a run that wrote a protected ref or bought a completion cannot validate at all.
 */
export interface Counts {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  attempted: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  kept: number;
  abandoned: CountsAbandoned;
  refused: CountsRefused;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  uncertain: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  processRuns: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  worktreesCreated: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  worktreesRemoved: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  branchesLeft: number;
  protectedRefWrites: 0;
  liveModelCalls: 0;
}


/**
 * Schema constraints this type cannot express: minLength=1
 */
export type FailurePatternInstancesItem = string;

/**
 * Schema constraints this type cannot express: minLength=1
 */
export type FailurePatternCohortsItem = string;

/**
 * One failure group. `evidence` is `systematic` only when the group spans at least two DISTINCT instances — recurrence across unrelated proposals is what separates evidence about the mechanism from evidence about one proposal.
 */
export interface FailurePattern {
  key: "mechanism" | "reason";
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  name: string;
  mechanism: "surface" | "budget" | "gate" | "measurement" | "command" | "effect";
  instances: Array<FailurePatternInstancesItem>;
  cohorts: Array<FailurePatternCohortsItem>;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  failures: number;
  evidence: "systematic" | "incidental";
}


/**
 * One scalar for the round — what a candidate mechanism must strictly beat to be accepted. Per-instance acceptance cannot see a change that fixes one case and breaks two others; this can.
 */
export interface AggregateScore {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  attempted: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  kept: number;
  /**
   * Schema constraints this type cannot express: minimum=0, maximum=1
   */
  value: number;
}


/**
 * What a ROUND of verdicts amounts to, above the per-experiment decision. A mechanism that changed itself once per observed failure would bend around whatever it happened to see; this block reports which failures recur across distinct instances and therefore license a mechanism-level repair at all.
 */
export interface Aggregate {
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  failures: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  systematic: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=0
   */
  incidental: number;
  /**
   * Schema constraints this type cannot express: minimum=0, maximum=1
   */
  systematicRatio: number;
  mechanisms: Array<FailurePattern>;
  reasons: Array<FailurePattern>;
  /**
   * One scalar for the round — what a candidate mechanism must strictly beat to be accepted. Per-instance acceptance cannot see a change that fixes one case and breaks two others; this can.
   */
  score: AggregateScore;
}


export interface EvolveSuitePackagesItem {
  /**
   * Schema constraints this type cannot express: pattern="^@(jarenjs|tangleai)/[a-z0-9-]+$"
   */
  name: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  version: string;
}


export interface EvolveSuite {
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  packages: Array<EvolveSuitePackagesItem>;
}


/**
 * The fixture repository as this run verified it: every committed file digest recomputed, and the base revision confirmed by materializing the tree and reading HEAD back. `materializationRuns` counts the git processes that cost, which are not experiment processes.
 */
export interface EvolveFixture {
  id: "evolve-fixture-rank/v1";
  licence: "MIT";
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  provenance: string;
  manifestRevision: Sha256;
  policyRevision: Sha256;
  strategiesRevision: Sha256;
  baseRevision: GitRevision;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  files: number;
  /**
   * Schema constraints this type cannot express: type="integer", minimum=1
   */
  materializationRuns: number;
}


export interface EvolveRegistrationProposalsItem {
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  id: string;
  /**
   * Schema constraints this type cannot express: minLength=1
   */
  strategyId: string;
  revision: Sha256;
  expect: Expectation;
}


/**
 * Immutable input. Editing any of it invalidates every measurement taken against it.
 */
export interface EvolveRegistration {
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  strategies: Array<Strategy>;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  proposals: Array<EvolveRegistrationProposalsItem>;
  experiments: 16;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  hostProbes: Array<"symlink-indirection" | "protected-ref-write" | "unknown-command" | "env-leak">;
  budgets: Budgets;
}


/**
 * What `benchmark/evolve.ts` writes. The registration is immutable input: a fixture repository pinned by file digests and one constant base revision, an immutable-surface policy and budget set held outside that repository, a hand-authored strategy library, and sixteen adversarial proposals each carrying the exact decision, reason and refusal code it must produce — true improvement, no-op, regression, flake, edits to tests, CI, the gate script, the registered threshold and the fitness instrument, a rename, a generated file, a path escape, an oversized patch, and three hostile runtimes. Rows are replaced by id as a mechanism lands and never change the registration or its denominators; `implementation-missing` is the honest state while no executor exists. Two analytic controls license every other number: an oracle that reads the registration back, and a seeded random selection. The census reconciles by `$query` at validation time, protected-ref writes and live model calls are literal zeros, and `reportId` is the canonical SHA-256 of the document with that field excluded. No clock, hostname, temporary path or duration has a representable member anywhere in this document.
 * Schema constraints this type cannot express: $query={"$and":[{"$eq":[{"$count":"$.registration.proposals[*].id"},{"$count":{"$distinct":"$.registration.proposals[*].id"}}]},{"$eq":[{"$count":"$.registration.proposals[*]"},"$.registration.experiments"]},{"$eq":[{"$count":"$.rows[*]"},"$.registration.experiments"]},{"$eq":[[{"$for":{"p":"$.registration.proposals[*]"},"$return":"$p.id"}],[{"$for":{"r":"$.rows[*]"},"$return":"$r.proposalId"}]]},{"$eq":[[{"$for":{"p":"$.registration.proposals[*]"},"$return":"$p.strategyId"}],[{"$for":{"r":"$.rows[*]"},"$return":"$r.strategyId"}]]},{"$eq":[{"$count":"$.hostProbes[*]"},{"$count":"$.registration.hostProbes[*]"}]},{"$every":{"r":"$.rows[?(@.state=='implementation-missing')]"},"$satisfies":{"$and":[{"$eq":["$r.actual",null]},{"$eq":["$r.matches",null]},{"$eq":["$r.effects.legs",0]},{"$eq":["$r.effects.unresolved",0]}]}},{"$every":{"r":"$.rows[?(@.state=='run')]"},"$satisfies":{"$and":[{"$ne":["$r.actual",null]},{"$ne":["$r.matches",null]}]}},{"$eq":[{"$count":"$.rows[?(@.state=='run')]"},"$.counts.attempted"]},{"$eq":[{"$count":"$.rows[?(@.actual.decision=='kept')]"},"$.counts.kept"]},{"$eq":[{"$count":"$.rows[?(@.actual.decision=='abandoned')]"},"$.counts.abandoned.total"]},{"$eq":[{"$count":"$.rows[?(@.actual.decision=='refused')]"},"$.counts.refused.total"]},{"$eq":[{"$count":"$.rows[?(@.actual.decision=='uncertain')]"},"$.counts.uncertain"]},{"$eq":[{"$sum":"$.counts.abandoned.byReason.*"},"$.counts.abandoned.total"]},{"$eq":[{"$sum":"$.counts.refused.byReason.*"},"$.counts.refused.total"]},{"$eq":["$.counts.attempted",{"$add":[{"$add":["$.counts.kept","$.counts.abandoned.total"]},{"$add":["$.counts.refused.total","$.counts.uncertain"]}]}]},{"$not":{"$lt":["$.counts.worktreesCreated","$.counts.worktreesRemoved"]}},{"$eq":[{"$count":"$.registration.proposals[*]"},"$.controls.oracle.attempted"]},{"$eq":[{"$count":"$.registration.proposals[?(@.expect.decision=='kept')]"},"$.controls.oracle.kept"]},{"$eq":[{"$count":"$.controls.oracle.strategies[*]"},{"$count":"$.registration.strategies[*]"}]},{"$eq":[{"$sum":"$.controls.oracle.strategies[*].attempted"},"$.controls.oracle.attempted"]},{"$eq":[{"$sum":"$.controls.oracle.strategies[*].kept"},"$.controls.oracle.kept"]},{"$eq":[{"$count":"$.controls.random.selected[*]"},"$.controls.random.selects"]},{"$eq":["$.controls.random.attempted","$.controls.random.selects"]},{"$or":[{"$eq":[{"$count":"$.rows[?(@.state=='implementation-missing')]"},0]},{"$eq":["$.decision","implementation-missing"]}]},{"$or":[{"$ne":["$.decision","implementation-missing"]},{"$not":{"$eq":[{"$count":"$.rows[?(@.state=='implementation-missing')]"},0]}}]},{"$or":[{"$ne":["$.decision","oracle-exact"]},{"$and":[{"$every":{"r":"$.rows[*]"},"$satisfies":{"$eq":["$r.matches",true]}},{"$every":{"p":"$.hostProbes[*]"},"$satisfies":{"$eq":["$p.state","pass"]}}]}]},{"$or":[{"$ne":["$.decision","executor-conformant"]},{"$every":{"r":"$.rows[*]"},"$satisfies":{"$eq":["$r.state","run"]}}]},{"$eq":[{"$add":["$.aggregate.failures","$.counts.kept"]},"$.counts.attempted"]},{"$eq":[{"$add":["$.aggregate.systematic","$.aggregate.incidental"]},"$.aggregate.failures"]},{"$eq":["$.aggregate.score.attempted","$.counts.attempted"]},{"$eq":["$.aggregate.score.kept","$.counts.kept"]},{"$every":{"p":"$.aggregate.mechanisms[*]"},"$satisfies":{"$eq":[{"$count":{"$distinct":"$p.instances[*]"}},{"$count":"$p.instances[*]"}]}}]}
 */
export interface Evolve {
  benchmark: "evolve";
  /**
   * Which report contract produced this document: the canonical digest of the schema it validates against.
   */
  instrument: { name: "evolve"; entry: "benchmark/evolve.ts"; contractRevision: Sha256; };
  source: SourceManifest;
  suite: EvolveSuite;
  /**
   * The fixture repository as this run verified it: every committed file digest recomputed, and the base revision confirmed by materializing the tree and reading HEAD back. `materializationRuns` counts the git processes that cost, which are not experiment processes.
   */
  fixture: EvolveFixture;
  /**
   * Immutable input. Editing any of it invalidates every measurement taken against it.
   */
  registration: EvolveRegistration;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  rows: Array<Row>;
  controls: Controls;
  /**
   * Schema constraints this type cannot express: minItems=1
   */
  hostProbes: Array<HostProbe>;
  counts: Counts;
  aggregate: Aggregate;
  /**
   * The shared config-identity envelope. Every row here is analysis: no provider stack ran, so no row may reference one.
   */
  identity: unknown;
  /**
   * `implementation-missing` while any registered row has no executor to run it; `executor-conformant` when every row ran; `oracle-exact` only when every row also produced its registered decision and every host probe passed.
   */
  decision: "implementation-missing" | "executor-conformant" | "oracle-exact";
  reportId: Sha256;
}


